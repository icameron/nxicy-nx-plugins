{
  "$schema": "http://json-schema.org/schema",
  "$id": "Module",
  "title": "Python module generator",
  "description": "Generates a basic python module in the python directory",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": ["name"]
}
