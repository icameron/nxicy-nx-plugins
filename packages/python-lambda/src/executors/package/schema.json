{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Package executor",
  "description": "Packages the function files and package dependencies into a zip file",
  "type": "object",
  "properties": {
    "functionPath": {
      "type": "string",
      "description": "The path of the function files.",
      "x-completion-type": "directory",
      "x-priority": "important"
    },
    "packages": {
      "description": "Package folders to include in the zip.",
      "type": "array",
      "items": {
        "type": "string",
        "description": "The path of the Package folder to include in the package.",
        "x-completion-type": "directory"
      },
      "default": []
    },
    "zipFilePath": {
      "type": "string",
      "description": "The path of the zip file",
      "x-completion-type": "directory",
      "x-priority": "important"
    },
    "extractPath": {
      "type": "string",
      "description": "The path to extract the zip file",
      "x-completion-type": "directory",
      "x-priority": "important",
      "default": ""
    }
  },
  "required": ["functionPath", "zipFilePath"]
}
